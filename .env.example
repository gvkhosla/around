# LLM — at least one
OPENROUTER_API_KEY=
OPENAI_API_KEY=
LLM_MODEL=openai/gpt-4o-mini

# Optional. Without it Semantic Scholar is ~1 request/second.
S2_API_KEY=

# Optional. Puts OpenAlex on the polite pool.
OPENALEX_MAILTO=

# Public URL used in bot replies and share links.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional. Shown on the homepage.
NEXT_PUBLIC_X_HANDLE=

# X bot (`bun run bot`) — needs tweet.read, users.read, tweet.write.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
